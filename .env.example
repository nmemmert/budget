# IMPORTANT: Generate a secure encryption key before deploying
# Run: openssl rand -base64 32
# Then replace the value below with your generated key

ENCRYPTION_KEY=change-this-to-a-secure-random-key

# Optional: Set custom port (default is 7654)
# PORT=7654

# Environment
NODE_ENV=production
